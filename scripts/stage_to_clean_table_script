Step 7: Load Clean data from Stage_Orders to Clean_Orders table
CREATE OR REPLACE PROCEDURE LOAD_CLEAN_DATA AS
v_count NUMBER;
BEGIN
INSERT INTO CLEAN_ORDERS 
SELECT * FROM STAGE_ORDERS
WHERE TOTAL_AMOUNT IS NOT NULL AND TOTAL_AMOUNT > 0;
v_count := SQL%ROWCOUNT;
END;
